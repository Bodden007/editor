#include "../includes/expDoc.hpp"

void expDoc::exportDocument()
{
    /*
     *Here the logic of document export is executed
     * if(export document == tru )
     * {
     * impd = true
     * }
     */
    expd = true;
}